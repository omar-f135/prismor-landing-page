# Dependencies
node_modules/

# Uploaded media files (too large for git, ephemeral on server)
uploads/

# Runtime data — never commit (production orders & user passwords live here)
data/orders.json
data/users.json
data/config.json

# macOS
.DS_Store
